import { HttpConfigInterceptor } from './httpconfig.interceptor';

describe('HttpConfigInterceptor', () => {
  it('should create an instance', () => {
    expect(new HttpConfigInterceptor()).toBeTruthy();
  });
});
